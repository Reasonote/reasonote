
/**
 * TODO:
 * 
 * This function should get existing activities matching the criteria.
 * 
 * These activities should be ordered by the following criteria:
 * 
 * Most important criteria:
 * - pinnedSkillIds
 * - level: 'INTRO' | 'BEGINNER' | 'INTERMEDIATE' | 'ADVANCED' | 'EXPERT'
 * - seenState?: 'unseen' | 'seen'
 * - gradeType?: 'ungraded' | 'graded'
 * - activityTypes?: ActivityType[]
 */
export async function getExistingActivities(){
    
}