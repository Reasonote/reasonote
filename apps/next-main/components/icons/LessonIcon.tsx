import {LocalLibrary} from "@mui/icons-material";

type LessonIconProps = React.ComponentProps<typeof LocalLibrary>;

export function LessonIcon(props: LessonIconProps) {
    return <LocalLibrary {...props} />;
}
  