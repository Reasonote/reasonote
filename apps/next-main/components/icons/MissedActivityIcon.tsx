import React from "react";

import {Rule} from "@mui/icons-material";
import {SvgIconProps} from "@mui/material";

export default function MissedActivityIcon(props: SvgIconProps) {
    return <Rule fontSize="small" {...props}/>
}