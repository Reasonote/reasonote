// TODO - nothing else should be imported here, but this should be the root source
// For all types that need these types.
export const ClientFieldParserTypeNames = [
    // Field overrides

    // Scalar overrides
    "JSON",
    "JSONB",
    "Date",
    "DateTime",
    "Datetime",
    "BigInt",
] as const;
